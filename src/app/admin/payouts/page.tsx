"use client";
import {useEffect,useState} from "react";
const peso=(n:number)=>`₱${(n/100).toFixed(2)}`;
export default function Payouts(){const [rows,setRows]=useState<any[]>([]),[start,setStart]=useState(""),[end,setEnd]=useState(""),[ref,setRef]=useState<Record<string,string>>({}),[msg,setMsg]=useState("");const load=()=>fetch('/api/admin/payouts').then(r=>r.json()).then(x=>setRows(x.data||[]));useEffect(()=>{load()},[]);async function act(id:string,a:string,body?:any){const r=await fetch(`/api/admin/payouts/${id}/${a}`,{method:'POST',headers:{'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined});const x=await r.json();if(!r.ok)setMsg(x.error||'Action failed');else setMsg('');load()}async function prepare(){const r=await fetch('/api/admin/payouts/prepare',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({periodStart:start,periodEnd:end})});const x=await r.json();setMsg(r.ok?`Prepared ${x.summary.ordersIncluded} orders`:x.error);load()}return <main className="space-y-6 p-8"><h1 className="text-3xl font-bold">Weekly payouts</h1><section className="flex flex-wrap items-end gap-3 rounded border bg-white p-4"><label>Start<input type="date" className="ml-2 rounded border p-2" value={start} onChange={e=>setStart(e.target.value)}/></label><label>End<input type="date" className="ml-2 rounded border p-2" value={end} onChange={e=>setEnd(e.target.value)}/></label><button className="rounded bg-blue-600 px-4 py-2 text-white" onClick={prepare}>Prepare new payout</button></section>{msg&&<p className="text-sm text-red-600">{msg}</p>}{rows.map(p=><details key={p.id} className="rounded border bg-white p-4"><summary className="cursor-pointer"><b>{p.station.name}</b> · {p.status} · {peso(p.netCentavos)} <span className="ml-4 text-sm text-slate-500">Gross {peso(p.grossCentavos)} · PayMongo fee {peso(p.processingFeeCentavos||0)} · Commission {peso(p.commissionCentavos)} · Held {peso(p.heldCentavos)} · Adjustments {p.adjustmentCentavos<0?`−${peso(-p.adjustmentCentavos)}`:peso(p.adjustmentCentavos)} · {p.periodStart.slice(0,10)}–{p.periodEnd.slice(0,10)}</span></summary><div className="mt-4 space-y-2">{p.payoutItems.map((i:any)=><p key={i.id} className="text-sm">Order {i.orderId} · {peso(i.grossCentavos)} gross · {peso(i.processingFeeCentavos||0)} PayMongo fee · {peso(i.commissionCentavos)} commission · {peso(i.netCentavos)} net</p>)}<div className="flex flex-wrap gap-2 pt-2">{p.status==='DRAFT'&&<button className="rounded bg-green-600 px-3 py-1 text-white" onClick={()=>act(p.id,'approve')}>Approve</button>}{p.status==='APPROVED'&&<button className="rounded bg-blue-600 px-3 py-1 text-white" onClick={()=>act(p.id,'process')}>Mark processing</button>}{p.status==='PROCESSING'&&<><input className="rounded border p-1" placeholder="Transfer reference" value={ref[p.id]||''} onChange={e=>setRef({...ref,[p.id]:e.target.value})}/><button className="rounded bg-green-600 px-3 py-1 text-white" onClick={()=>act(p.id,'pay',{transferReference:ref[p.id]})}>Mark paid</button><button className="rounded bg-red-600 px-3 py-1 text-white" onClick={()=>act(p.id,'fail',{failureMessage:'Manual transfer failed'})}>Mark failed</button></>}</div></div></details>)}</main>}
